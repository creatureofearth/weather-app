import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danpowellb.app',
  appName: 'Weather App',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
